export const MovielistHeading = ({ heading }) => {
  return (
    <div className="col">
      <h1>{heading}</h1>
      <h2>
        Add a new movie to your watchlist by searching it on the top right
        corner!
      </h2>
    </div>
  );
};
